spring:
  datasource:
    url: jdbc:h2:mem:testdb

  jpa:
    generate-ddl: false
#    hibernate:
#      ddl-auto: none
#      ddl-auto: create

    properties:
      hibernate:
        format_sql: true

    show-sql: true

  sql:
    init:
      mode: never

  flyway:
    enabled: true


#logging:
#  level:
#    ROOT: ERROR
